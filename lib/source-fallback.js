'use strict';
function ageMin(now,t){const a=new Date(now).getTime(),b=new Date(t).getTime();return Number.isFinite(a)&&Number.isFinite(b)?Math.max(0,(a-b)/60000):Infinity;}
function select(sources=[],opts={}){const now=opts.now||new Date().toISOString(),maxAgeMin=Number(opts.maxAgeMin||5);const ordered=[...sources].sort((a,b)=>(a.priority||999)-(b.priority||999));const eligible=ordered.filter(s=>s&&s.ok!==false&&s.sourceAt&&ageMin(now,s.sourceAt)<=maxAgeMin);if(!eligible.length)return {state:'OFFLINE',selected:null,checkedAt:now,candidates:ordered.map(s=>({name:s.name,ageMin:ageMin(now,s.sourceAt),ok:s.ok!==false}))};const selected=eligible[0],primary=ordered[0];return {state:selected===primary?'PRIMARY':'FALLBACK',selected,checkedAt:now,candidates:ordered.map(s=>({name:s.name,ageMin:ageMin(now,s.sourceAt),ok:s.ok!==false}))};}
function recovery({previousSource,currentSource}={}){if(previousSource&&currentSource&&previousSource!==currentSource&&currentSource==='primary')return 'RECOVERED_PRIMARY';if(previousSource&&currentSource&&previousSource!==currentSource)return 'SOURCE_CHANGED';return 'UNCHANGED';}
module.exports={ageMin,select,recovery};
